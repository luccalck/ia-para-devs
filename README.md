
  # High-Fidelity Prototype for EdTech

  This is a code bundle for High-Fidelity Prototype for EdTech. The original project is available at https://www.figma.com/design/MUfLttDDRzkG1WaINIkFQT/High-Fidelity-Prototype-for-EdTech.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  